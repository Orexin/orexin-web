//var testCall = require('../src/js/main').testFunct;
var assert = require('chai').assert;

describe('App', () => {
	it('Build passing: ', () => {
		let result = true;
		assert.equal(result, true);
	});
});
